UIWEB (React + TypeScript + Vite)

This folder will host the web client.

Quick start (after Vite scaffolding):

- Install: `npm install`
- Run dev: `npm run dev`
- Build: `npm run build`
- Preview: `npm run preview`
